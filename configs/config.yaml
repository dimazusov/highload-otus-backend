server:
  http:
    host: "0.0.0.0"
    port: "8080"

redis:
  address: "redis:6379"
  password: ""

db:
  mysql:
    dialect: "mysql"
    dsn: "mysql://db:db@mysql:3306/highload?serverVersion=5"
    maxConnLifetimeMinute: 3
    maxConn: 10

repository:
  type: "mysql"
