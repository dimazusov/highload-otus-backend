server:
  http:
    host: "0.0.0.0"
    port: "80"

redis:
  address: "redis:6379"
  password: ""

db:
  mysql:
    dialect: "mysql"
    dsn: "db:db@tcp(mysql:3306)/highload?charset=utf8mb4"
    maxConnLifetimeMinute: 3
    maxConn: 10

repository:
  type: "mysql"
